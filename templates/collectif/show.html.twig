{% extends 'base.html.twig' %}

{% block title %}Collectif{% endblock %}

{% block body %}
    <h1>Collectif</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ collectif.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ collectif.name }}</td>
            </tr>
            <tr>
                <th>History</th>
                <td>{{ collectif.history }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_collectif_index') }}">back to list</a>

    <a href="{{ path('app_collectif_edit', {'id': collectif.id}) }}">edit</a>

    {{ include('collectif/_delete_form.html.twig') }}
{% endblock %}
